name: First PR

on:
  pull_request:
    types:
      - opened
      - reopened

permissions:
  contents: read
  issues: read
  pull-requests: write

jobs:
  first-pr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        id: get-pr-count
        with:
          script: |
            const [owner, repo] = '${{ github.repository }}'.split('/');
            const reply = await github.rest.issues.listForRepo({
              state: 'all',
              repo: repo,
              owner: owner,
              creator: '${{ github.actor }}'
            })
            return reply.data.filter((data) => data.pull_request).length;
      - if: ${{ steps.get-pr-count.outputs.result }} == '1'
        run: |
          yarn dlx -p @o3r/azure-tools o3r-comment-pr "
            ![](https://raw.githubusercontent.com/AmadeusITGroup/otter/main/assets/logo/flavors/fireworks.png)
            Thanks so much for opening your first PR here!
          "
